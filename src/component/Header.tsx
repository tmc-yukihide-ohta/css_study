import styles from './Header.module.scss'
const Header = () => {
  return (
    <div className={styles.headerComponent}>
      <h1>CSSの単位（px, em, rem, %, vw, vh など）について</h1>
    </div>
  )
}

export default Header
